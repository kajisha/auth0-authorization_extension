RSpec.describe Auth0::AuthorizationExtension do
  it "has a version number" do
    expect(Auth0::AuthorizationExtension::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
